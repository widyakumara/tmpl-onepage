---
import { PageFoot, PageHead } from "@components";
import config from "@config";
import "../styles/index.css";

const { site } = config;
const { props } = Astro;

const title = props.title
  ? `${props.title} • ${site.title}`
  : `${site.title}: ${site.tagline}`;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <title>{title}</title>
  </head>
  <body class:list={["page", props.page]}>
    <div id="root">
      <PageHead title={site.title} />
      <div id="body"><slot /></div>
      <PageFoot title={site.title} year={site.year} tagline={site.tagline} />
    </div>
  </body>
</html>
